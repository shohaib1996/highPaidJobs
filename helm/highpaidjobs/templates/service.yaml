apiVersion: v1
kind: Service
metadata:
  name: highpaidjobs
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: highpaidjobs
  type: NodePort
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
